<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Trip.App.Views.AddTripPage"
             xmlns:viewModel="clr-namespace:Trip.App.ViewModels"
             xmlns:models="clr-namespace:Trip.Data.DbModels;assembly=Trip.Data"
             x:DataType="viewModel:AddTripPageViewModel"
             BackgroundColor="#F5F7FA"
             Title="Add New Trip">

    <ScrollView>
        <VerticalStackLayout Padding="20"
                             Spacing="25"
                             HorizontalOptions="Center"
                             WidthRequest="380">

            <!-- Cím -->
            <Label Text="Add New Trip"
                   FontSize="26"
                   FontAttributes="Bold"
                   TextColor="#2D3748"
                   HorizontalTextAlignment="Center"/>

            <!-- Form kártya -->
            <Frame Padding="20"
                   CornerRadius="20"
                   HasShadow="True"
                   BackgroundColor="White">

                <VerticalStackLayout Spacing="20">

                    <!-- Trip Name -->
                    <VerticalStackLayout>
                        <Label Text="Trip Name"
                               FontAttributes="Bold"
                               FontSize="15"
                               TextColor="#4A5568"/>
                        <Entry Placeholder="e.g. Summer in Paris"
                               FontSize="15"
                               TextColor="#2D3748"
                               PlaceholderColor="#A0AEC0"
                               Text="{Binding Name}"
                               BackgroundColor="#EDF2F7"
                               HeightRequest="40"/>
                    </VerticalStackLayout>

                    <!-- Start Date -->
                    <VerticalStackLayout>
                        <Label Text="Start Date"
                               FontAttributes="Bold"
                               FontSize="15"
                               TextColor="#4A5568"/>
                        <DatePicker Date="{Binding StartDate}"
                                    Format="yyyy-MM-dd"
                                    BackgroundColor="#EDF2F7"
                                    TextColor="#2D3748"
                                    HeightRequest="40"/>
                    </VerticalStackLayout>

                    <!-- End Date -->
                    <VerticalStackLayout>
                        <Label Text="End Date"
                               FontAttributes="Bold"
                               FontSize="15"
                               TextColor="#4A5568"/>
                        <DatePicker Date="{Binding EndDate}"
                                    Format="yyyy-MM-dd"
                                    BackgroundColor="#EDF2F7"
                                    TextColor="#2D3748"
                                    HeightRequest="40"/>
                    </VerticalStackLayout>

                    <!-- Destination Picker -->
                    <VerticalStackLayout>
                        <Label Text="Destination"
                               FontAttributes="Bold"
                               FontSize="15"
                               TextColor="#4A5568"/>
                        <Picker ItemsSource="{Binding Destinations}"
                                ItemDisplayBinding="{Binding Name}"
                                SelectedItem="{Binding SelectedDestination, Mode=TwoWay}"
                                Title="Select a destination"
                                BackgroundColor="#EDF2F7"
                                TextColor="#2D3748"
                                HeightRequest="70"/>
                    </VerticalStackLayout>

                    <!-- Submit button -->
                    <Button Text="Save Trip"
                            Command="{Binding AddTripCommand}"
                            FontSize="16"
                            FontAttributes="Bold"
                            TextColor="White"
                            BackgroundColor="#3182CE"
                            CornerRadius="25"
                            HeightRequest="48"
                            HorizontalOptions="Center"
                            WidthRequest="220"/>

                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>